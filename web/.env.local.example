# Optional, for `npm run dev` only. The shipped UI is served by the agentflow
# daemon and calls its API on the same origin, so a build needs no settings.
# When running `next dev` on its own port, point it at a local daemon:
# NEXT_PUBLIC_AGENTD_URL=http://127.0.0.1:4344
